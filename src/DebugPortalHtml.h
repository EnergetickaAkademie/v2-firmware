#ifndef DEBUG_PORTAL_HTML_H
#define DEBUG_PORTAL_HTML_H

#include <Arduino.h>

// Keep the complete offline portal page separate from its HTTP handlers.
extern const char PORTAL_HTML[] PROGMEM;

#endif
